name: BENZINO Institutional Scanner

on:
  schedule:
    # 15m scans every 15 minutes, after each candle close
    - cron: "2,17,32,47 * * * *"

    # 1h scans once every hour, after the hourly candle has closed
    - cron: "7 * * * *"

    # 4h scans once every 4 hours, after the 4h candle has closed
    - cron: "17 */4 * * *"

    # 1d scans once daily, after the daily candle has closed
    - cron: "37 0 * * *"

  workflow_dispatch:
    inputs:
      full_scan:
        description: "Run full master watchlist scan"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

      scan_timeframes:
        description: "Timeframes to scan on manual runs"
        required: true
        default: "15m"
        type: choice
        options:
          - "15m"
          - "1h"
          - "4h"
          - "1d"
          - "15m,1h,4h,1d"

concurrency:
  group: benzino-institutional-scanner
  cancel-in-progress: false

permissions:
  contents: read

env:
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

  DEFAULT_USER_TIMEFRAME: "1h"
  FULL_UNIVERSE_SCAN_EVERY_HOURS: "0"
  SCAN_MASTER_WATCHLIST_FALLBACK: "true"

  SCHEDULE_CRON: ${{ github.event.schedule || 'manual' }}

  FORCE_FULL_SCAN: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.full_scan || 'false' }}
  SCAN_ALL_TIMEFRAMES_EVERY_RUN: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.full_scan || 'false' }}

  SCAN_TIMEFRAMES: >-
    ${{ github.event_name == 'schedule'
        && (github.event.schedule == '2,17,32,47 * * * *' && '15m'
        || github.event.schedule == '7 * * * *' && '1h'
        || github.event.schedule == '17 */4 * * *' && '4h'
        || github.event.schedule == '37 0 * * *' && '1d')
        || github.event.inputs.scan_timeframes
        || '15m' }}

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Show trigger details
        run: |
          echo "event_name=${{ github.event_name }}"
          echo "schedule=${{ github.event.schedule }}"
          echo "scan_timeframes=${{ env.SCAN_TIMEFRAMES }}"
          date -u

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements_scanner.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_scanner.txt

      - name: Run scanner
        run: python scanner.py
